<UserControl x:Class="OrthoSpineAI.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="400">
    <Grid Background="White">
        <Border Width="360" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="White" CornerRadius="8"
                Effect="{StaticResource CardShadow}" Padding="32">
            <StackPanel>
                <TextBlock Text="OrthoSpine AI" FontSize="26" FontWeight="Bold"
                           Foreground="#1565C0" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                <TextBlock Text="Logowanie do systemu" FontSize="13" Foreground="#757575"
                           HorizontalAlignment="Center" Margin="0,0,0,24"/>

                <TextBlock Text="Login" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14" Padding="8,6" Margin="0,0,0,12"
                         BorderBrush="#BDBDBD" BorderThickness="1"/>

                <TextBlock Text="Hasło" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                <PasswordBox x:Name="PasswordBox" FontSize="14" Padding="8,6"
                             BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,8"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#C62828"
                           FontSize="12" TextWrapping="Wrap" Margin="0,0,0,8"
                           Visibility="{Binding ErrorMessage,
                               Converter={StaticResource StringToVisibilityConverter}}"/>

                <Button Content="Zaloguj się"
                        Command="{Binding LoginCommand}"
                        FontSize="14" Padding="0,10"
                        Background="#1565C0" Foreground="White"
                        BorderThickness="0" Cursor="Hand" Margin="0,8,0,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <ProgressBar IsIndeterminate="True" Height="3" Margin="0,12,0,0"
                             Visibility="{Binding IsBusy,
                                 Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
