<UserControl x:Class="OrthoSpineAI.UI.Views.PatientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Pacjenci" FontSize="22" FontWeight="Bold" Foreground="#1565C0"
                       VerticalAlignment="Center"/>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="↻ Odśwież" Command="{Binding RefreshCommand}"
                        Background="#E3F2FD" Foreground="#1565C0"
                        Padding="14,8" BorderThickness="0" Cursor="Hand" Margin="0,0,8,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="+ Nowy pacjent" Command="{Binding AddPatientCommand}"
                        Background="#1565C0" Foreground="White"
                        Padding="16,8" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- Live search bar -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="13" Padding="34,8,8,8"
                     BorderBrush="#BDBDBD" BorderThickness="1"
                     VerticalContentAlignment="Center"/>
            <!-- Search icon overlay -->
            <TextBlock Text="🔍" FontSize="13" Foreground="#9E9E9E"
                       HorizontalAlignment="Left" VerticalAlignment="Center"
                       Margin="10,0,0,0" IsHitTestVisible="False"/>
            <!-- Placeholder -->
            <TextBlock Text="Szukaj po nazwisku, imieniu lub PESEL…"
                       FontSize="13" Foreground="#BDBDBD"
                       HorizontalAlignment="Left" VerticalAlignment="Center"
                       Margin="34,0,0,0" IsHitTestVisible="False">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- Error message -->
        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="#C62828" FontSize="12" Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Patient list -->
        <Grid Grid.Row="3">
            <!-- Empty state -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Patients.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="🔍" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Brak pacjentów spełniających kryteria." FontSize="14"
                           Foreground="#9E9E9E" HorizontalAlignment="Center"/>
            </StackPanel>

            <DataGrid ItemsSource="{Binding Patients}"
                      SelectedItem="{Binding SelectedPatient}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      SelectionMode="Single" CanUserReorderColumns="False"
                      CanUserResizeRows="False" RowHeight="38"
                      BorderThickness="1" BorderBrush="#E0E0E0"
                      Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName, Mode=OneWay}" Width="*"/>
                    <DataGridTextColumn Header="Imię" Binding="{Binding FirstName, Mode=OneWay}" Width="*"/>
                    <DataGridTextColumn Header="PESEL" Binding="{Binding PESEL, Mode=OneWay}" Width="130"/>
                    <DataGridTextColumn Header="Wiek" Binding="{Binding AgeYears, Mode=OneWay}" Width="60"/>
                    <DataGridTextColumn Header="Miasto" Binding="{Binding AddressCity, Mode=OneWay}" Width="120"/>
                    <DataGridTemplateColumn Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Wybierz →"
                                        Command="{Binding DataContext.SelectPatientCommand,
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent" Foreground="#1565C0"
                                        BorderThickness="0" Cursor="Hand" FontSize="12"
                                        FontWeight="SemiBold"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <ProgressBar IsIndeterminate="True" Height="3" VerticalAlignment="Top"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
