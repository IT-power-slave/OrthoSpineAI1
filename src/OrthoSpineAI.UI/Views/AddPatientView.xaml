<UserControl x:Class="OrthoSpineAI.UI.Views.AddPatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="600">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nowy pacjent" FontSize="22" FontWeight="Bold"
                   Foreground="#1565C0" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Imię *" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,6" BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Nazwisko *" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,6" BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="PESEL" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Pesel, UpdateSourceTrigger=PropertyChanged}"
                         Padding="8,6" BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Data urodzenia" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                        <DatePicker SelectedDate="{Binding BirthDate}"
                                    Padding="8,6" Margin="0,0,0,12"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Płeć" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding SexOptions}"
                                  SelectedItem="{Binding Sex}"
                                  Padding="8,6" Margin="0,0,0,12"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Ulica i nr" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                <TextBox Text="{Binding AddressSt, UpdateSourceTrigger=PropertyChanged}"
                         Padding="8,6" BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Miasto" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding AddressCity, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,6" BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Kod pocztowy" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding ZipCode, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,6" BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#C62828"
                           FontSize="12" TextWrapping="Wrap"
                           Visibility="{Binding ErrorMessage,
                               Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Anuluj" Command="{Binding CancelCommand}"
                    Padding="20,8" Margin="0,0,12,0"
                    Background="#E0E0E0" BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Zapisz pacjenta" Command="{Binding SaveCommand}"
                    Padding="20,8" Background="#1565C0" Foreground="White"
                    BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
