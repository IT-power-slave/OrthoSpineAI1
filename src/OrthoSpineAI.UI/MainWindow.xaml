<Window x:Class="OrthoSpineAI.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="OrthoSpine AI" Height="700" Width="1000"
        MinHeight="600" MinWidth="800"
        Background="#F5F5F5">

    <!-- DataContext is MainViewModel injected from App.xaml.cs -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Border Grid.Row="0" Background="#1565C0" Padding="16,8">
            <DockPanel>
                <TextBlock Text="OrthoSpine AI" FontSize="20" FontWeight="Bold"
                           Foreground="White" VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center"
                            Visibility="{Binding Shell.IsLoggedIn,
                                Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding Shell.CurrentUserName, StringFormat='👤 {0}'}"
                               Foreground="#B3E5FC" FontSize="13" VerticalAlignment="Center"
                               Margin="0,0,16,0"/>
                    <Button Content="🏠 Panel" Command="{Binding Shell.GoToDashboardCommand}"
                            Background="#0D47A1" Foreground="White"
                            BorderThickness="0" Padding="12,4" Margin="0,0,8,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="Pacjenci" Command="{Binding Shell.GoToPatientsCommand}"
                            Background="#0D47A1" Foreground="White"
                            BorderThickness="0" Padding="12,4" Margin="0,0,8,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="Wyloguj" Command="{Binding Shell.LogoutCommand}"
                            Background="#0D47A1" Foreground="White"
                            BorderThickness="0" Padding="12,4" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Page area — DataTemplates route to correct View -->
        <ContentControl Grid.Row="1"
                        Content="{Binding Shell.CurrentPage}"
                        Margin="0"/>
    </Grid>
</Window>
